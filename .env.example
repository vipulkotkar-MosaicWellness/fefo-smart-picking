# Copy this file to ".env" and paste your Supabase project values.
# Found in Supabase dashboard -> Project Settings -> API.
# If these are left blank, the app runs in LOCAL mode (data saved in the
# browser only) so you can demo it without a backend.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
